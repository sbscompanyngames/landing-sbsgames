/** @type {import('next').NextConfig} */
const nextConfig = {
    // Add the following configuration to disable image optimization
    images: {
      unoptimized: true,
    },
  };
  
  export default nextConfig;
  